# /assets/

Reserved for any downloadable or miscellaneous static files that don't belong
in `css/`, `js/`, `images/`, or `data/` — for example a press kit, a resume, or
a one-off document. Nothing lives here yet.
